#! /bin/cbc -r

goto std

$bf_add$charry:charry:charry$,>,[-<+>]<.$

classy cbclass1:
	nummy abc

classy cbclass2 -> evvy:
	nummy abc

nummy cloudberry (nummy argc, lissy[strry] argv):
	cbclass1 abcd
	# for loop
	for(0 <= i < 10):
		// this is also a for loop
		for(5 < j <= 8):
			/* prints something */
			print(i, j, bf_add(i, j))
			abcd.abc += 1
	print(abcd.abc)
